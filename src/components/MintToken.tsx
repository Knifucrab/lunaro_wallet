// MintToken feature removed. This file is intentionally left as a placeholder
// to avoid breaking imports. The UI and logic for minting test tokens have
// been removed to focus on real mainnet token support.

import type { FC } from 'react';

const _RemovedMintToken: FC = () => null;

export default _RemovedMintToken;
